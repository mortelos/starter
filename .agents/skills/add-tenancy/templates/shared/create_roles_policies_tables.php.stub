<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * Roles + policies as owner-editable DB data (D11).
 *
 * Placement depends on the driver:
 *  - database driver -> put this file in database/migrations/tenant/ so each
 *    tenant database gets its own roles/policies (the tenant DB is the scope).
 *  - row driver       -> put it in database/migrations/, add a `tenant_id`
 *    column to BOTH tables and the BelongsToTenant trait to the models, so rows
 *    are scoped within the shared database.
 *
 * Deny-by-default is enforced by the gate (absence of an `allow` row = deny),
 * not by a column default.
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::create('roles', function (Blueprint $table): void {
            // For the row driver, also: $table->string('tenant_id')->index();
            $table->string('id')->primary();
            $table->string('name');
            $table->string('description')->nullable();
            $table->json('trust_config')->nullable();
            $table->timestamps();
        });

        Schema::create('policies', function (Blueprint $table): void {
            // For the row driver, also: $table->string('tenant_id')->index();
            $table->string('id')->primary();
            $table->string('role_id');
            $table->string('action');
            $table->string('effect')->default('deny'); // 'allow' | 'deny'
            $table->timestamps();

            $table->index(['role_id', 'action']);
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('policies');
        Schema::dropIfExists('roles');
    }
};
